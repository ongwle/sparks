<%= simple_form_for(@trainer) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
	  <%= f.input :TrainerName %>
	  
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
